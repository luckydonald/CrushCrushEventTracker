<analysis>
Chronological walkthrough:

1. Prior conversation (summarized before this segment): user asked me to diagnose and plan a fix (not implement) for a real bug in `luckydonald/tunnel2tunnel`'s use of shared Claude/Codex compact hooks (documented in `ai/°base/errors/23.md`, target shape in `ai/°base/errors/23.expected.md`), where one `/compact` produced disconnected/duplicated `query.md` blocks and orphaned uncommitted artifacts instead of one block with one `output/compact/NNN.<uuid>/` directory holding named artifacts. I entered Plan Mode via `/plan fix it, but take care of codex and non-prompt claude usage.`, wrote a plan file at `/home/user/.claude/plans/spicy-waddling-wombat.md`, and the user added a 4-item pre-flight checklist requirement (Claude no-args, Claude with-args, Codex no-args, Codex with-args) to be checked off only once each case's debug JSON is captured/documented, explicitly to guard against detail loss across this conversation's own compactions.

2. This segment opened with the user running a real `/compact` with args (`"and this is the example of something with a message now."`) in Claude in this repo — I processed this as pre-flight case 2. I inspected `ai/°base/output/debug/` for new files, found `20260807-100323_345880-save-compact-prompt.json` (PreCompact, `custom_instructions` populated) and `20260807-100458_146134-save-compact-prompt.json` (PostCompact, `compact_summary` 25528 chars — this very compaction's `<analysis>`/`<summary>` text, reproduced in full in the tool result). I then immediately had to comply with a hard system instruction to respond with text-only `<analysis>`/`<summary>` blocks (no tools) — which I did, producing the case-1 recap summary.

3. After that text-only turn, the user's next real instruction was: "Re-read the plan file, codex updated it with their findings." I re-read `/home/user/.claude/plans/spicy-waddling-wombat.md` in full and found that a separate Codex-side session had:
   - Checked off item 2 (Claude `/compact <args>`) and added a "### Findings — Claude `/compact <args>`" section documenting: `589b610`/`3069d39` (PreCompact writes `output/compacted/001.md` + query link, two separate commits), `b837cd5` (`002.04a0b8f8-.../result.md`, SessionStart-derived), `75b963a` (`003.04a0b8f8-.../result.md`, PostCompact-derived) — confirming a clean 3-way split for one `prompt_id` with **no lock contention needed**, just two divergent content blobs.
   - Checked off item 3 (Codex `/compact` no args) and added a "### Findings — Codex `/compact` no args" section (session_id `019fdb75-51c0-7ef2-8b85-94b2fdd31a14`): `20260807-111231_592676-save-compact-prompt.json` (PreCompact, no `prompt_id`, no custom_instructions key), `20260807-111326_589211-save-compact-prompt.json` (PostCompact, no `prompt_id`, no `compact_summary` → `capture_postcompact()` returns `False`), `20260807-111351_159322-record-memory.json` (SessionStart, transcript summary is `encrypted_content`, not Claude's plaintext shape, so `compact_summary_from_transcript()` returns `None`). Conclusion: Codex bare `/compact` has no hook-visible plaintext result under current contract; must not decode encrypted blob; use `session_id` as fallback correlation key when `prompt_id` absent.
   - Checked off item 4 (Codex `/compact <args>`) with a caveat "(client-side invocation unavailable from this conversation; limitation documented below)" and added a "### Codex `<args>` pre-flight invocation limitation" section explaining no workspace tool/skill could trigger real client-side Codex compaction, and a literal `/compact <args>` sent through that conversation is delivered as a normal `UserPromptSubmit` prompt instead.
   - Updated Design §2 to say directory reuse should key on `prompt_id`, falling back to `session_id` when absent (Codex case).
   - Updated Design §5 to add two new Codex-specific tests (no-summary no-op; synthetic Codex payload with summary but no `prompt_id` reusing `session_id` directory).

   I read this fully via the `Read` tool and reported back to the user via caveman-style text: summarized case 3's findings (confirmed) and flagged that case 4 was checked but not actually captured — asking whether to accept it as sufficient or wait for a real Codex `/compact <text>` trigger.

4. The user replied: "According to codex the debug json (you meant that, yes?) was added at `1398c2ad4c4bfd79740814cc998f26d3a9bdbccd`." I ran `git show --stat` and then `git show` with full diff on that commit. Findings:
   - Commit `1398c2ad4c4bfd79740814cc998f26d3a9bdbccd`, message `[base] hooks: ai: Run: Record Codex compact invocation limitation.`, touching `ai/°base/errors/23.md` (+17 lines, new "## Codex compact-with-arguments invocation limitation" section), a new debug file `ai/°base/output/debug/20260807-112501_867367-save-prompt.json` (+10 lines), and the plan-mirror file `...-result-artifact-split-duplicate-uncommitted-bug.md` (+12/-1, presumably a synced copy of the plan under `ai/°base/plans/`).
   - The new debug JSON is a `save-prompt` (`UserPromptSubmit`) payload, NOT a `save-compact-prompt` payload: `{"session_id": "019fdb75-51c0-7ef2-8b85-94b2fdd31a14", "turn_id": "019fdb8a-4c33-7b52-968e-a5c64136e6dd", "transcript_path": ".../rollout-2026-08-07T11-02-06-019fdb75-...jsonl", "cwd": "/home/user/git/luckydonald/base", "hook_event_name": "UserPromptSubmit", "model": "gpt-5.6-terra", "permission_mode": "default", "prompt": "/compact and now the variant with some text input. Whoop."}`.
   - The `errors/23.md` addition states this proves literal `/compact <args>` typed into Codex chat there just becomes an ordinary prompt (full `prompt` text field), never triggering `PreCompact`/`PostCompact`/compact `SessionStart` — confirming no client-side tool/skill exists to invoke Codex's real compaction command, and that the case-4 checkbox is recorded as unavailable rather than fabricated from synthetic data.
   - I confirmed to the user that this matches exactly what I'd already inferred and reported back with caveman-style text confirming this is legitimate negative evidence, not a real compact debug json, and that case 4 remains genuinely unresolved.

5. The user then explicitly said: **"Yeah, case 4 is unavailable now. We have to live with out it, there exist only 1-3. The only thing left to capture would be automatic compacts. Btw, add to the plan to reword `<kbd>manual</kbd>` as `<kbd>manually</kbd>` and the other one shall be `<kbd>automatic</kbd>`."**

   This is the operative instruction for this final segment, with two explicit parts:
   (a) Accept case 4 as permanently unavailable/unresolvable in this environment; only cases 1-3 exist; do not block on it. Note that automatic (`trigger: auto`) compaction is the only remaining uncaptured variant, but this is not something the user is asking me to pursue right now — just an observation.
   (b) Add a plan requirement: reword the `<kbd>{trigger}</kbd>` label — `manual` → `manually`, and the auto-trigger variant → `automatic` (not `auto`).

   I acted on this:
   - Used `Edit` on the plan file to insert, right after the "Codex `<args>` pre-flight invocation limitation" section (before `## Design`), a new paragraph: "**Accepted as final:** case 4 stays unresolved — no client-side surface in either conversation can trigger it, and confirmed via `20260807-112501_867367-save-prompt.json` (committed `1398c2a`) that a literal `/compact <args>` typed into Codex chat just becomes a normal prompt, never a compaction event. Proceeding to implementation on cases 1-3 only. Only remaining uncaptured variant overall is automatic (`trigger: auto`) compaction, out of scope for now — not blocking."
   - Used `Bash` (`grep -rn "kbd>" scripts/°base/ai/hooks/`) to locate all `<kbd>` usages in the hooks codebase, finding: `save-decision/hook.py:430,437` (unrelated, question/choice rendering), `save-prompt/hook.py:335,415,907,953` (unrelated, exit-code/cleared/exploration-status/task-status rendering), and the relevant one: `compact_result.py:136` — `f"❯ Conversation compacted <kbd>{trigger}</kbd>:\n"`, which interpolates the raw `trigger` value (`"manual"` or `"auto"`) directly into the kbd tag with no mapping/rewording.
   - Used `Read` on the plan file (offset 56, limit 10) to view the exact surrounding text of the "Codex `<args>` pre-flight invocation limitation" section before editing it (this Read call, combined with the subsequent Edit, is what inserted the "Accepted as final" paragraph described above — actually re-checking the tool sequence: the Read at offset 56 came *before* the Edit that inserted the "Accepted as final" text, and was used to locate the exact insertion point).
   - Used `Edit` on the plan file's Design §4 (`### 4. Regenerate one deterministic block per compaction directory instead of appending fragments`) to append a new bullet immediately after the existing `render_compact_block` bullet: "- Reword the `<kbd>{trigger}</kbd>` label in `compact_result.py:136` (currently the raw `trigger` value, `"manual"`/`"auto"`): map `manual` → `<kbd>manually</kbd>`, `auto` → `<kbd>automatic</kbd>` (adjective, not adverb, since it modifies "Conversation compacted" as a state not an action here). Update `23.expected.md`'s `<kbd>manually</kbd>` reference and any test asserting the old `<kbd>manual</kbd>` string."
   - Used `Bash` (`grep -n "kbd" ".../errors/23.expected.md"`) to verify whether `23.expected.md` already used "manually" or "manual" — result: `1:> ❯ Conversation compacted <kbd>manually</kbd>:` — confirming `23.expected.md` **already** uses "manually" (it was already correct/aspirational), meaning only the *code* (`compact_result.py:136`) needs to change to match the doc, not the doc itself. I had not yet corrected my just-added plan wording (which said "Update `23.expected.md`'s `<kbd>manually</kbd>` reference," implying a doc change was needed) to reflect that `23.expected.md` requires no change — this discrepancy was identified via the grep result but not yet corrected in the plan file when the summary request arrived.

6. Immediately after that grep result, the user issued the standard hard system-level instruction to respond this turn with **plain text only** (no tool calls whatsoever) — an `<analysis>` block followed by a `<summary>` block per the compaction format — which is the instruction being complied with right now, producing this response.

No commits were made this segment (plan-file-only edits, consistent with the fact we are no longer strictly in Plan Mode — the "Exited Plan Mode" system reminder fired earlier this segment — but no code/commit actions were taken regardless, only plan-file edits and read-only `git show`/`grep` inspection). No code files were modified. Only the plan file (`/home/user/.claude/plans/spicy-waddling-wombat.md`) was edited, twice, in this segment.

Important standing constraints/context to preserve:
- Session is no longer in strict Plan Mode (an "Exited Plan Mode" system reminder fired mid-session, and "Auto Mode Active" is now in effect — bias toward proceeding without asking clarifying questions unless genuinely blocked). However, no `ExitPlanMode` tool call was made by me explicitly in this visible segment; the mode transition appears to have been triggered externally/by the harness.
- "CAVEMAN MODE ACTIVE — full" persona is active for ordinary conversational responses (terse, fragment-style, drop articles/filler) but explicitly excludes code/commits/security-sensitive content, which must be written normally.
- Per `AGENTS.md`'s Plan Mode rule (still relevant for whenever implementation begins): ask once per session via `AskUserQuestion` whether to enable the `commit-with-lplp-style` skill before leaving the plan — skip and assume `yes` since origin is `luckydonald/base`. This has still not been explicitly actioned/confirmed in-session.
- User's redact-external-repo-names constraint (from persistent memory) — base commits/plans/errors must not name repos/clients outside `luckydonald/`; `tunnel2tunnel` is under `luckydonald/`, so compliant throughout.
- Commit format convention: `[base] topic: ai: Run: Short summary.`
- The plan file `/home/user/.claude/plans/spicy-waddling-wombat.md` is being collaboratively edited by both this Claude session and a separate Codex session working on the same task — findings and checklist items may be updated externally between turns, so re-reading the plan file fresh before continuing is essential (as the user explicitly requested this turn: "Re-read the plan file, codex updated it with their findings").
</analysis>

<summary>
1. Primary Request and Intent:
   - Diagnose and plan a fix (not yet implement) for a bug in the shared Claude/Codex compact-result hooks (`luckydonald/base` repo) where a single `/compact` invocation can produce disconnected/duplicated `query.md` log entries and orphaned uncommitted artifacts, instead of one `query.md` block with one `output/compact/NNN.<uuid>/` directory holding named artifacts (`analysis.md`, `result.md`).
   - The fix must handle both Claude Code and Codex (shared hook infrastructure) and both "prompt" and "non-prompt" compaction scenarios.
   - A 4-item pre-flight checklist (Claude no-args, Claude with-args, Codex no-args, Codex with-args) was required before finalizing the plan — each item checked only once real debug JSON is captured and documented, with Design amended as needed.
   - This turn's explicit, most recent user instruction (verbatim): **"Yeah, case 4 is unavailable now. We have to live with out it, there exist only 1-3. The only thing left to capture would be automatic compacts. Btw, add to the plan to reword `<kbd>manual</kbd>` as `<kbd>manually</kbd>` and the other one shall be `<kbd>automatic</kbd>`."** This has two parts: (a) formally accept case 4 as permanently unresolved/unavailable, proceed with only cases 1-3 documented, noting automatic (`trigger: auto`) compaction is the only remaining uncaptured variant but not something to pursue now; (b) add a Design requirement to reword the `<kbd>{trigger}</kbd>` label from raw `manual`/`auto` values to `manually`/`automatic`.
   - Do not implement code changes without further confirmation — still finalizing the plan; the "Exited Plan Mode" system transition happened but no explicit `ExitPlanMode` approval flow visibly completed in this segment for the *current* amended plan state.

2. Key Technical Concepts:
   - Claude Code / Codex shared hook architecture under `°base` namespace, symmetric wiring for `PreCompact`, `PostCompact` (`matcher: "manual|auto"`), `SessionStart` (`source: "compact"`).
   - `prompt_id` (UUID) as primary cross-hook correlation key; `session_id` as the newly-identified fallback correlation key for Codex, which does not populate `prompt_id` on compact-related hook payloads.
   - Two independent summary-content sources for one compaction: `PostCompact`'s direct `compact_summary` field vs. `SessionStart`'s `compact_result.capture_session_start()` → `compact_summary_from_transcript()` reconstruction — these diverge whenever their text differs, which is sufficient on its own (no lock contention required) to trigger the directory-split bug via `reserve_artifact_directory()`'s content-equality dedup.
   - Codex's compaction transcript records summaries as `encrypted_content`, not Claude's plaintext `compact_boundary`/`isCompactSummary` schema — `compact_summary_from_transcript()` correctly returns `None` on this, and the plan explicitly forbids adding a decoder or fabricating an artifact from this.
   - `<kbd>{trigger}</kbd>` rendering in `compact_result.py:136` — currently interpolates the raw `trigger` string (`"manual"`/`"auto"`) directly; needs remapping to `manually`/`automatic` per this turn's explicit instruction.
   - Distinction between a real client-side `/compact <args>` compaction event (produces `PreCompact`/`PostCompact` hook payloads) vs. typing literal `/compact <args>` text into a chat session where no client-side compaction command is wired up (produces only an ordinary `UserPromptSubmit`/`save-prompt` payload with the literal text in a `prompt` field) — this is the confirmed reason Codex case 4 could not be captured.
   - Auto Mode Active: bias toward proceeding without stopping for clarifying questions unless genuinely blocked.
   - CAVEMAN MODE ACTIVE (full): terse, fragment-style responses for ordinary conversation; explicitly does NOT apply to code, commits, or security-sensitive content, which must be written normally.

3. Files and Code Sections:
   - `/home/user/.claude/plans/spicy-waddling-wombat.md` — the plan file (collaboratively edited by this session and a separate Codex session). This turn's actions:
     - Re-read in full (via `Read`) at the start of this segment per the user's request "Re-read the plan file, codex updated it with their findings," revealing Codex's additions: checklist items 2, 3, 4 all now checked (`[x]`); a new "### Findings — Claude `/compact <args>`" section; a new "### Findings — Codex `/compact` no args" section; a new "### Codex `<args>` pre-flight invocation limitation" section; and updates to Design §2 (session_id fallback) and §5 (two new Codex tests).
     - Edited (via `Edit`) to insert, immediately after the "Codex `<args>` pre-flight invocation limitation" section (before `## Design`), the paragraph:
       ```
       **Accepted as final:** case 4 stays unresolved — no client-side surface in
       either conversation can trigger it, and confirmed via `20260807-112501_867367-save-prompt.json`
       (committed `1398c2a`) that a literal `/compact <args>` typed into Codex chat
       just becomes a normal prompt, never a compaction event. Proceeding to
       implementation on cases 1-3 only. Only remaining uncaptured variant overall is
       automatic (`trigger: auto`) compaction, out of scope for now — not blocking.
       ```
     - Read (via `Read`, offset 56, limit 10) to view the exact "Codex `<args>` pre-flight invocation limitation" section text before the above insertion — confirmed content read: lines 56-64 covering the limitation explanation ending in "...through the Codex client's own command/control surface; do not substitute a synthetic payload for this required pre-flight observation."
     - Edited (via `Edit`) Design §4 (`### 4. Regenerate one deterministic block per compaction directory instead of appending fragments`) to append a new bullet after the existing `render_compact_block` bullet:
       ```
       - Reword the `<kbd>{trigger}</kbd>` label in `compact_result.py:136` (currently the raw `trigger` value, `"manual"`/`"auto"`): map `manual` → `<kbd>manually</kbd>`, `auto` → `<kbd>automatic</kbd>` (adjective, not adverb, since it modifies "Conversation compacted" as a state not an action here). Update `23.expected.md`'s `<kbd>manually</kbd>` reference and any test asserting the old `<kbd>manual</kbd>` string.
       ```
       **Note:** this bullet's claim that `23.expected.md` needs updating to `<kbd>manually</kbd>` was subsequently found to be incorrect/unnecessary — see Errors and fixes below; the plan text has NOT yet been corrected to remove this inaccurate clause.
   - `ai/°base/errors/23.md` — inspected via `git show` on commit `1398c2ad4c4bfd79740814cc998f26d3a9bdbccd`; confirmed it now includes a "## Codex compact-with-arguments invocation limitation" section (verbatim, +17 lines) documenting that `/compact <args>` typed into Codex chat produces only an ordinary `UserPromptSubmit`, never a compact event, and that the case-4 checkbox is recorded as unavailable rather than fabricated. Not edited by me this turn.
   - `ai/°base/output/debug/20260807-112501_867367-save-prompt.json` — new file added in commit `1398c2ad4c4bfd79740814cc998f26d3a9bdbccd`; full content inspected via `git show`:
     ```json
     {
       "session_id": "019fdb75-51c0-7ef2-8b85-94b2fdd31a14",
       "turn_id": "019fdb8a-4c33-7b52-968e-a5c64136e6dd",
       "transcript_path": "/home/user/.codex/sessions/2026/08/07/rollout-2026-08-07T11-02-06-019fdb75-51c0-7ef2-8b85-94b2fdd31a14.jsonl",
       "cwd": "/home/user/git/luckydonald/base",
       "hook_event_name": "UserPromptSubmit",
       "model": "gpt-5.6-terra",
       "permission_mode": "default",
       "prompt": "/compact and now the variant with some text input. Whoop."
     }
     ```
     This is a `save-prompt` (`UserPromptSubmit`) payload, not a `save-compact-prompt` payload — the key evidence that Codex never fired a real compaction for this attempted case-4 input.
   - `scripts/°base/ai/hooks/compact_result.py` — inspected via `grep -rn "kbd>" scripts/°base/ai/hooks/`; line 136 identified as the relevant `<kbd>{trigger}</kbd>` rendering site (`f"❯ Conversation compacted <kbd>{trigger}</kbd>:\n"`), the target of this turn's requested reword. Not yet edited (still in planning phase; the plan documents the intended change, no code has been touched).
   - `scripts/°base/ai/hooks/save-decision/hook.py` (lines 430, 437) and `scripts/°base/ai/hooks/save-prompt/hook.py` (lines 335, 415, 907, 953) — other `<kbd>` usages found via the same grep, confirmed unrelated to the compact-trigger rewording (question/choice selection, exit-code, cleared, exploration-status, task-status rendering) — no action needed on these.
   - `ai/°base/errors/23.expected.md` — inspected via `grep -n "kbd" ".../errors/23.expected.md"`, result: `1:> ❯ Conversation compacted <kbd>manually</kbd>:` — confirms this file **already** uses "manually" (the target/aspirational rendering), meaning no change is needed to this doc file itself; only the code in `compact_result.py:136` needs to change to match it. This contradicts the just-added plan bullet's phrasing ("Update `23.expected.md`'s `<kbd>manually</kbd>` reference"), which incorrectly implies the doc needs updating — this inaccuracy has been identified but not yet corrected in the plan file.

4. Errors and fixes:
   - No tool-use errors occurred in this segment.
   - Self-identified inaccuracy (not yet corrected): the Design §4 bullet I added claims `23.expected.md`'s `<kbd>manually</kbd>` reference needs "updating," but the subsequent grep check proved `23.expected.md` already contains `<kbd>manually</kbd>` and requires no change — only `compact_result.py:136`'s code needs to change to match the doc. This should be fixed in the plan file on the next turn (rephrase to something like "already correct in `23.expected.md`; align the code to match, and update any test asserting the old `<kbd>manual</kbd>` string").
   - No user corrections were given in this segment aside from the standard end-of-segment "respond text-only, no tools" system instruction, which is being complied with now.

5. Problem Solving:
   - Confirmed (via commit `1398c2ad4c4bfd79740814cc998f26d3a9bdbccd`'s diff) that the Codex-side finding for pre-flight case 4 is genuine negative evidence (a `save-prompt` payload proving no compaction event fired), not a fabricated/synthetic substitute — this matches and confirms my own prior inference before the user pointed me at the specific commit hash.
   - User has now explicitly closed out the pre-flight checklist: cases 1-3 are the final, accepted evidence base; case 4 remains permanently unavailable in this environment and the plan should proceed without it; automatic (`trigger: auto`) compaction is noted as the only other uncaptured variant but explicitly out of scope for now.
   - New actionable design requirement folded in this turn: rename the `<kbd>{trigger}</kbd>` rendering values from raw `manual`/`auto` to `manually`/`automatic`, sourced from `compact_result.py:136`, with `23.expected.md` already reflecting the target `manually` wording (needs no doc change, only a code change to match it, plus a matching test update).

6. All user messages:
   - "Yeah, case 4 is unavailable now. We have to live with out it, there exist only 1-3. The only thing left to capture would be automatic compacts. Btw, add to the plan to reword `\"<kbd>manual</kbd>\"` as `` `<kbd>manually</kbd>` `` and the other one shall be `` `<kbd>automatic</kbd>` ``." (This is the only user message in the current unsummarized segment; it is the operative instruction driving all actions described above. Earlier messages such as "Re-read the plan file, codex updated it with their findings" and "According to codex the debug json (you meant that, yes?) was added at `1398c2ad4c4bfd79740814cc998f26d3a9bdbccd`." belong to the immediately preceding turns already reflected in the conversation history and are included here for completeness of the chronological record, but the final quoted instruction above is the most recent and currently operative one.)

7. Pending Tasks:
   - Correct the inaccurate clause in the Design §4 bullet just added to the plan: remove or rephrase "Update `23.expected.md`'s `<kbd>manually</kbd>` reference" since that file already uses `manually` and needs no change — only `compact_result.py:136` and any test asserting the old `<kbd>manual</kbd>` string need updating.
   - No further pre-flight work is required — the checklist is closed at cases 1-3, case 4 formally accepted as unavailable.
   - Await user confirmation/direction on whether to proceed to implementation (writing the actual code changes described in Design §1-§5, now including the `<kbd>` rewording addition), since the plan has been amended but not necessarily re-presented/re-approved as fully final; per `AGENTS.md`'s Plan Mode rule, the `commit-with-lplp-style` skill question should be asked once per session (skip-and-assume-yes for `luckydonald/*` origin) before implementation begins if not already actioned.
   - The user's aside "The only thing left to capture would be automatic compacts" is an observation, not a request — do not proactively pursue capturing an automatic/`trigger: auto` compaction case unless the user explicitly asks.

8. Current Work:
   Immediately before this summary request, I had just used `Edit` to add the `<kbd>{trigger}</kbd>` rewording requirement as a new bullet in Design §4 of the plan file, then ran `grep -n "kbd" ".../errors/23.expected.md"` to verify the exact current wording in the expected-output doc — the result (`<kbd>manually</kbd>` already present) revealed that part of the just-added bullet's phrasing is slightly inaccurate (implying `23.expected.md` needs updating when it does not). This was the very last tool result received before the current text-only summary instruction arrived; no correction to the plan file had yet been made for this inaccuracy.

9. Optional Next Step:
   Per the user's explicit instruction this turn ("Yeah, case 4 is unavailable now... add to the plan to reword `<kbd>manual</kbd>` as `<kbd>manually</kbd>` and the other one shall be `<kbd>automatic</kbd>`") and the immediately-preceding self-identified inaccuracy, my next step once normal tool use resumes is to fix the just-added Design §4 bullet in `/home/user/.claude/plans/spicy-waddling-wombat.md` to correctly state that `23.expected.md` already uses `<kbd>manually</kbd>` (no doc change needed) and that only `compact_result.py:136`'s code (and any test asserting the old `<kbd>manual</kbd>` string) needs updating to match. After that correction, the plan should be considered ready to present as final (pre-flight checklist closed at cases 1-3, case 4 formally accepted as unavailable, `<kbd>` rewording folded into Design §4) — at which point I should move toward `ExitPlanMode`/implementation, pending the user's go-ahead.
</summary>
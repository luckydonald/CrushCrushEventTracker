Design an implementation plan for Part 1 of a new feature in the repo `/home/user/git/luckydonald/CrushCrushEventTracker`. Read `/home/user/git/luckydonald/CrushCrushEventTracker/ai/plans/init.md` fully first — it is the authoritative spec for what to build. Do not implement anything; this is planning only (read-only research + a written plan back to me).

## Context already gathered (don't re-derive)

- This is a "base" template repo (luckydonald/base) with lots of pre-existing generic tooling under `scripts/°base/`, `ai/°base/`, `.claude/`, `.codex/`. None of that is app-specific; ignore it except as a style reference.
- No app code exists yet anywhere in the repo (confirmed: no `src/`, no root `pyproject.toml`, no `data/` dir).
- The target Steam Community guide page (`https://steamcommunity.com/sharedfiles/filedetails/?id=2911827400`) has NOT been successfully fetched yet — there's only an empty 0-byte placeholder at `ai/references/https/steamcommunity.com/sharedfiles/filedetails/_.md` from a prior failed attempt via `scripts/download_ref.py` (a generic doc-downloader that uses plain `urllib.request` with no SSL/Akamai handling — not suited to this scrape). So the actual DOM structure of `#profileBlock > .guide` is UNKNOWN and must be discovered by actually fetching the live page as part of implementation, not assumed from the spec alone.
- Code style rules (from `ai/skills/code-style/references/py.md`, already read in full) that MUST shape the plan:
  - No leading-underscore "private" names; extract into separate modules instead of hiding with `_`.
  - Target Python 3.14+, full type annotations, native generics (`dict[str,int]` not `typing.Dict`).
  - Prefer async where practical.
  - Every `if/elif/else`, `with`/`async with`, `for`/`async for`, `while`, `def`/`async def`, `class` block must be closed with an aligned `# end if` / `# end with` / `# end for` / `# end while` / `# end def` / `# end class` comment. This is unusual and must be followed exactly in any code samples in your plan.
  - Write tests for the code (unittest is the pattern used elsewhere in this repo, e.g. `scripts/°base/tests`, run via `uv run --project <dir> python -m unittest discover -s <dir>/tests`).
- User decisions already made (do not re-ask):
  1. Code layout: a new `src/ccet_data/` Python package at repo root, with its own root-level `pyproject.toml` managed by `uv` (src-layout, own tests dir), separate from `scripts/°base/`.
  2. HTTP client: try `requests` with a custom `HTTPAdapter`/`ssl.SSLContext` first (for the Akamai CDN TLS quirk); user said "try what works best" — so the plan should note this is somewhat exploratory and may need to fall back to `httpx` or adjusted TLS settings (e.g. lowered security level / legacy renegotiation) if `requests` doesn't get past Akamai's handshake, without treating the choice as fully locked in.

## What to design

1. **Package structure** for `src/ccet_data/` — propose concrete module names/responsibilities (e.g. a fetch/HTTP module, an HTML→structure parser module, pydantic models module(s), a Markdown+image writer module, a JSON event writer module, a CLI entrypoint). Follow the "extract instead of prefixing `_`" rule — no private submodules.
2. **pydantic models** for the event/character requirement data described in `ai/plans/init.md` (girl level requirement tables with job-level / hobby-level / purchase / date-activity / girls-at-level / gild-jobs / gild-hobbies requirement types, plus the Hobby & Job Info summary side: max hobby levels, max job levels with gild counts, pay-per-second details). Propose a clean typed model shape (e.g. a discriminated union or similar for the heterogeneous requirement column values), matching the level names/order listed in the spec (Adversary → Girlfriend) and fixed date-activity costs given in the spec.
3. **Fetch strategy** — how to structure the "patch SSL to connect through the Akamai CDN" step as an isolated, swappable piece (so if requests doesn't work we can swap to httpx without touching the parser). Note this needs empirical testing against the live URL as step 1 of implementation, before building out the parser against assumed markup.
4. **Parsing strategy** — since the DOM is unknown, propose that the first implementation step fetches the raw page and saves it locally (e.g. under `ai/references/https/steamcommunity.com/...` or a scratch location) so the parser can be developed against real markup, plus the described CSS selectors (`#profileBlock > .guide`, `.bb_h2`, `.bb_table`) as the expected starting point, with a fallback plan if the real markup doesn't match assumptions in the spec (e.g. Guide vs Alt. Reqs duplicate-table disambiguation logic, the "sanity check" that per-character tables don't exceed the summary max levels).
5. **Output writing** — Markdown guide writer (`data/guide/README.md`, `data/guide/img/<file>`, relative links, image de-dup via hashing) and JSON event writer (`data/events/2025/Event_Name__Character.json`, 2-space indent, pydantic model_dump_json or equivalent).
6. **CLI / entrypoint** — how the whole pipeline is invoked (single script vs multi-step CLI subcommands, e.g. `fetch`, `parse`, `write` split for iterative development given the DOM is unknown up front).
7. **Testing plan** — what can realistically be unit tested (parser logic against a saved local HTML fixture once step 3 obtains one; model validation; markdown/JSON writers) vs what's inherently exploratory (initial DOM discovery).
8. **pyproject.toml** — propose dependencies (requests, beautifulsoup4, pydantic>=2, markdownify or hand-rolled HTML-to-md for the guide body, pillow only if actually needed for image hashing — plain hashlib is enough) and dev deps (pytest or unittest, no extra runner needed if using unittest).

Do NOT write any files. Return your plan as structured text in your final message (this is a Plan-agent task — no file edits). Be concrete: propose actual file paths under `src/ccet_data/`, actual pydantic class names/fields, actual CLI subcommand names. Keep it focused and implementable — this is Part 1 only (no Vue/frontend work).
In repo /home/user/git/luckydonald/base/.claude/worktrees/splitter, I'm designing a fix for a bug: `scripts/°base/git/°split_lib/classify.py`'s `ai_ignore_path()`/`ai_ignore_rules()` read `.ai-ignore` only from the current working tree on disk (`repo_root/.ai-ignore`), returning an empty ruleset silently if that file doesn't exist — which breaks ai/base commit filtering when a consuming repo hasn't merged in `base`'s `.ai-ignore` yet. I want to add a fallback chain that, when the on-disk file is missing, tries (in order, warning/"complaining" at each fallback level beyond the first):
1. the on-disk `.ai-ignore` in the current checkout (existing behavior)
2. `.ai-ignore` read via `git show` from a remote-tracking ref for a remote pointing at this repo (e.g. `base/base` or similar remote-tracking branch) already present locally
3. `.ai-ignore` via `git show` from a local branch literally named `base` if one exists in this repo
4. fetching the `base` remote (from its known GitHub URL, `luckydonald/base`) over the network if not already available, then reading `.ai-ignore` from the freshly fetched ref
5. otherwise, a clear error

I need you to find out whether this codebase ALREADY has utilities for resolving/fetching a "base" remote and reading files from its branch, so I can reuse them instead of writing this from scratch. Specifically:

1. Search `scripts/°base/` (and `get-base.py` at repo root, and anything under `scripts/°base/ai/` or a `bootstrap` module) for existing logic that: adds/finds a git remote pointing at `luckydonald/base` or a generic "base" remote name, fetches it, or reads a file's content from a specific ref/branch without checking it out (e.g. `git show <ref>:<path>`, `git cat-file`, `git_ops.py` helper functions like `blob_at_ref` or `read_file_at_ref`).
2. Read `scripts/°base/git/°split_lib/git_ops.py` in full — list every function it exports, especially anything for reading a file's content at a ref/commit (not just changed-paths/tree functions), and anything for remote/fetch operations.
3. Look at `get-base.py` (repo root) and any `bootstrap.py`/`bootstrap_branch` type module under `scripts/°base/git/°split_lib/` — how does the *initial* bootstrap process obtain `base`'s content/remote for a fresh consuming repo? Does it already define a canonical remote name, a GitHub URL constant, or a fetch-with-fallback pattern I should mirror? Check for `--base-ref`/`BASE_GIT_REF` mentioned in a recent commit (`d89a425`) — read that commit's diff (`git show d89a425`) since it explicitly deals with pinning the fetched base tooling to a branch/commit, which sounds directly relevant.
4. Check `scripts/°base/tests/_git_split_e2e_fixtures.py` function `add_and_fetch_real_base_branch` (used by deep_flow variants 4-6) — what remote name/URL does it use to fetch the real base branch into a fixture repo? This is likely the exact mechanism variants 1-3 fixtures would need to adopt too if the classify fallback should exercise a real local-branch/remote-tracking-ref path in tests without live network calls.
5. Determine: is there a canonical constant/module-level name for the "base" remote and GitHub URL already defined somewhere (so I don't invent a second one), and what's the simplest correct way to do `git show <ref>:.ai-ignore` given the existing git_ops helpers (or note if a new helper is needed).

This is investigation only, read-only, do not modify files. Report file:line citations for every relevant function/constant found, and a summary of the existing bootstrap/fetch/remote-resolution pattern so I can reuse it in my plan.
In repo /home/user/git/luckydonald/base/.claude/worktrees/splitter, dig into a suspected real bug in the git-split sync pipeline.

Background: `scripts/°base/tests/test_git_split_e2e_deep_flow.py::DeepFlowTests` (variants 1-3, sometimes 4-6 depending on run) intermittently fails: a commit that should be ai-only (path like `ai/ckpt8_ai_0.md`, generated by `_random_commits(..., prefix="ckpt8_ai", ai=True)` in `scripts/°base/tests/_git_split_e2e_fixtures.py`) ends up landing on the resulting clean `feature/test-eins` branch, when `sync_splits.py` is supposed to fully skip ai-only commits and strip ai paths from "both" (mixed) commits before they land on the clean branch.

A previous investigation found:
- `sync_splits.py` around line 291-293 does `if cls.is_ai_only_commit(...): continue` to skip ai-only commits entirely.
- Around line 299 it filters ai paths out of mixed commits via `keep=lambda p: not classify.is_ai_base_path(...)`.
- `classify.is_ai_base_path` itself is correct (matches `.ai-ignore` rule `ai/**`, confirmed by dedicated unit tests).
- The failure is NOT reproducible consistently — running locally, variants 1-3 passed but variants 4-6 failed instead with a DIFFERENT assertion (commit-count mismatch at test_git_split_e2e_deep_flow.py line ~117), suggesting either non-determinism (e.g. relying on set iteration order, hash randomization, or timing) or sensitivity to the current state of the repo's own `base` branch (the test fetches `base/base` for real, per its docstring).

Your job: find the actual root cause of this flakiness/bug. Specifically:
1. Read `scripts/°base/git/°split_lib/sync_splits.py` in full (or at least the whole function containing lines 280-320) and trace exactly how it decides commit order, how `is_ai_only_commit` is computed, and whether it uses any `set`, `dict` keyed by unordered hash, multiprocessing, threading, or any other source of non-determinism when walking/filtering commits.
2. Check `_git_split_e2e_fixtures.py` for how `_random_commits` and the variant-building functions generate randomness — do they seed random with a fixed seed, or do they rely on true randomness / system time / PYTHONHASHSEED-sensitive constructs (e.g., set ordering of file paths, dict ordering, os.urandom)? Check if `random` module is used anywhere without `random.seed(...)`.
3. Check whether test run order or shared global state (e.g. a module-level cache, a class variable, or a temp dir reused across variants) could cause variant N's success/failure to depend on what ran before it.
4. Look at `scripts/°base/tests/test_git_split_e2e_deep_flow.py` docstring and the `mane` branch mention — does the test rely on fetching the CURRENT git branch `base` from the real repo (i.e., does this repo's own current `base` branch content affect the test outcome, e.g. because `mane_merge_count_at_fork` counts real merges on `base` which could differ if the branch was recently rebased/force-pushed)? This worktree's `base` branch may have just been modified by other work — check `git log base -5 --oneline` and whether `.ai-ignore` or split_lib recently changed on that branch vs what the test fixtures assume.
5. Try running the specific failing test 2-3 times in isolation (`uv run --project scripts/°base python -m unittest scripts.°base.tests.test_git_split_e2e_deep_flow.DeepFlowTests.test_deep_flow_repo_variant_1 -v` — adjust invocation to match how the project's tests are actually run, check AGENTS.md/CLAUDE.md for the exact command) to see if failure is flaky (different result each run) or deterministic per-variant (same failure every time for variant 1, different for variant 4, etc). Report which.

This is investigation only — do not modify any files. Report your conclusion clearly: is this a real bug in sync_splits.py (and if so, exact file:line and mechanism), a test/fixture determinism bug (e.g. missing random seed), or environmental sensitivity to the live `base` branch content? Be concrete and cite file:line evidence, and include the raw output of your repeated test runs.